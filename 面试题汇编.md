# 智能体与大模型应用 · 面试题汇编

> **目标岗位**：**AI 智能体 / 大模型应用开发工程师**（偏工程落地：RAG、Agent、工具与 MCP、编排、观测与成本，而非纯算法训练）。  
> **使用方式**：优先按 **附录 B「岗位冲刺路径」** 刷题；答题时对照下文 **「让考官听懂的表述习惯」**。

## 文档说明

### 让考官听懂的表述习惯（应用面通用）

1. **先结论，后展开**：第一句用业务语言说「解决什么问题」，第二句再落到技术栈（如 RAG / FC / MCP）。
2. **分三层说清**：**数据怎么进**（索引/权限）→ **运行时怎么走**（检索/工具/多轮）→ **怎么验收**（指标、降级、回滚）。
3. **主动讲权衡**：延迟 vs 成本、自动化 vs 安全（HITL）、自研 vs 平台——面试官往往在听你的**判断力**。
4. **项目题用 STAR**：情境 → 任务 → **你具体做了什么** → **量化或可验证结果**（时延、准确率、故障次数）。
5. **不会可拆解**：「这块我还没深入到底层，但我理解上层流程是……」比编造类名安全。

### 重要度图例（题目旁标注）

| 标记       | 含义     | 说明                                                         |
| ---------- | -------- | ------------------------------------------------------------ |
| **⭐⭐⭐** | **核心** | 智能体应用岗**极高频**；答不好容易暴露工程经验不足。       |
| **⭐⭐**   | **高频** | 中高级岗、落地与架构追问常见。                               |
| **⭐**     | **进阶** | 架构深挖、安全、多智能体、特定业务域；时间紧可后补。         |

### 章节结构说明（按知识域归类）

| 归类               | 章节编号 | 内容概要                                                           |
| ------------------ | -------- | ------------------------------------------------------------------ |
| **基础与选型**     | 0 ～ 3   | 轻量训练认知、应用选型、提示词、上下文与记忆                       |
| **RAG 全链路**     | 4 ～ 6   | 索引/检索/生成、进阶分块与 Agentic RAG、向量与 Embedding           |
| **框架与编排**     | 7 ～ 12  | LangChain/LangGraph、低代码、Function Calling、工具生态            |
| **Agent 与 MCP**   | 13 ～ 16 | Agent 架构、多智能体、**MCP**、Agentic 综合                        |
| **治理与运维**     | 17 ～ 19 | 安全、评测观测、成本性能                                           |
| **垂直场景（选读）** | 20 ～ 23 | Deep Research、NL2SQL、客服、代码沙箱与浏览器（按 JD 选看）      |
| **面试实战**       | 24       | 项目话术、Java/Python、手搓框架、**§88** 问答题（无营销经历可跳过抽奖类） |
| **附录**           | A、B、C  | 速记表、**岗位冲刺索引**、新手入门                               |

---

## 0、大模型基础认知（应用岗：答清「边界」即可）

> **模块重要度：⭐**（应用岗常被问「你们用不用微调」「和 RAG 啥关系」，不必展开数学推导）

### 0.1 预训练、SFT、RLHF / DPO 分别解决什么问题？【⭐】

| 阶段        | 解决问题（口语）                                     |
| ----------- | ---------------------------------------------------- |
| **预训练**  | 模型有没有语言能力与通用知识                         |
| **SFT**     | 模型会不会**按指令格式**、按角色说话                 |
| **RLHF**    | 对齐人类偏好（更安全、更有用），经典路径含奖励模型+PPO |
| **DPO**     | 另一类对齐思路：直接用偏好数据优化策略，**工程上常比 RLHF 链路轻** |

**应用岗收尾**：线上多数业务用 **API 模型 + RAG/Agent**；**微调**多在「口吻、领域格式、分类头」等场景才上；**训练细节**可交给算法同事，你要讲清**何时不建议微调**（数据少、要快速迭代知识时用 RAG）。

---

## 1、大模型应用基础与工程选型

> **模块重要度：⭐⭐⭐**（应用岗开场题 + **云上 API 治理**常追问，与 **17～19** 章连贯）

### 1. 用一句话说明：什么时候用提示词、RAG、微调、智能体？【⭐⭐⭐】

**选型三问**：① 缺的是「知识」还是「行为」？缺知识 → RAG；缺行为 → 微调。② 要不要自动查资料、调 API、执行多步？要 → 智能体。

**速记**：提示词 = 知识已在模型内；RAG = 先检索再生成；微调 = 改行为/风格；智能体 = 大模型 + 真正执行工具。

---

### 2. RAG 和微调的主要区别与组合方式？【⭐⭐】

| 维度     | RAG                | 微调                 |
| -------- | ------------------ | -------------------- |
| 知识更新 | 改文档/索引即可    | 需重新训练或增量流程 |
| 可解释性 | 可引用片段         | 难直接溯源           |
| 典型成本 | 检索与上下文 token | 数据与算力           |
| 风险     | 检索错/上下文噪声  | 灾难性遗忘、过拟合   |

**组合**：RAG 特别适合融合新知识；微调能优化输出格式、提升复杂指令执行能力。工程上多数场景用「RAG + 提示词」或「RAG + 微调」即可；复杂系统可加 Agent 做检索路由。

**微调 vs 续训**：微调改「行为方式」，续训改「知识分布」。**顺序**：先续训再微调；多数场景 RAG + 微调即可，续训成本高。

**常追问：你们项目为什么没上微调？【⭐⭐】**  
**答法**：先肯定微调价值，再讲**约束**——数据量/标注成本、迭代周期、知识时效（政策常变 → RAG 改索引更快）、团队分工（应用侧优先把 RAG+Agent+评测跑稳）。若已有微调，则说明**目标**（格式/领域分类/语气）与**评测方式**，避免空泛。

---

### 3. 大模型应用上线前，你会关注哪些工程指标？【⭐⭐】

**参考答案**：延迟（首 token / 端到端）、可用性、错误率、token 成本、并发与限流、上下文长度、数据与隐私合规、可观测性（trace/log）、版本与回滚（模型/Prompt/索引）。

**加分**：若有 SLA/SLO，可补充 **P95/P99**、超时策略、降级（换小模型/只检索不生成）、配额与多租户隔离；安全侧补充 **审计留痕**与敏感数据脱敏。

**常追问：调用云上大模型 API 时你怎么做工程治理？【⭐⭐⭐】**（与 **60**、**63** 呼应）

- **超时与重试**：区分连接超时与读超时；429/5xx 用**指数退避 + 抖动**，设**上限次数**；**幂等键**（业务 request_id）避免重试导致重复副作用（若下游非幂等则慎重重试）。
- **流式**：SSE/WebSocket 断线要有**最后序号/Checkpoint**或降级为一次性返回；网关 idle 超时与**心跳**对齐。
- **安全与合规**：密钥放配置中心/密钥管理，**日志脱敏**（不全量打 Prompt/PII）；按租户做**配额与限流**。
- **可观测**：每次调用带 **trace_id**，记录模型名、版本、token 用量、延迟分位，便于成本和故障归因。

---

### 4. 什么是「幻觉」？在应用层如何缓解？【⭐⭐】

**定义**：模型可能「已读乱回」「已读不回」或「似是而非」——生成看似合理但与事实不符的内容。

**缓解（按落地优先级口述）**：

1. **RAG + 引用**：检索片段进上下文，并要求**标明来源**；无证据时**拒答或明确不确定**。
2. **生成约束**：低温度、强模板（表格/JSON）、**「只能根据上述材料回答」**类指令；与 **7**、**34** 结合。
3. **工具与校验**：计算、查库走工具；输出经 **schema/规则校验**，失败则重试或降级。
4. **人机协同**：高风险场景 **HITL**；运营抽检与 **badcase 回灌** 到评测集（**14**、**64**）。

**考官想听的点**：幻觉无法「彻底消灭」，工程目标是**可检测、可降级、可复盘**。

---

## 2、提示词工程与模型 I/O

> **模块重要度：⭐⭐⭐**（与 `1-2`、`11`、`13`、`14` 等章呼应）

### 5. 系统提示词与用户提示词一般如何分工？【⭐⭐⭐】

- **System**：角色、边界、工具使用规范、输出格式、安全策略（稳定、少改）。
- **User / 对话**：具体任务、上下文补充、多轮澄清。

**本仓库表述**：结构化提示词 = 把稳定约束放 **System**，把动态约束放 **User**。**六要素**：角色、任务、背景、输入数据、输出格式、质量与约束；多数场景「角色 + 任务 + 输出格式」即可。**Zero-shot**：无示例直接问；**Few-shot**：先给几个示例再让模型仿写。**落地**：System 外置配置 + 版本管理。

---

### 6. 如何做 Prompt 版本管理与回归？【⭐⭐】

**要点**：与代码同仓库或配置中心管理；变更打版本；用**固定评测集**（golden questions）做回归；记录模型名、温度、top_p、模板 hash。**注意**：换底层模型后，Prompt 一般需重新调优。

---

### 7. 结构化输出有哪些实现方式？各有什么代价？【⭐⭐】

**常见路径**：JSON Schema / Function Calling 约束；解析器二次校验（如 Pydantic）；失败重试；必要时「先输出 JSON 再解析」的专用小模型。**本仓库**：提示词六要素中的「输出格式」可指定表格、JSON、代码等；LCEL 链中 `prompt | model | parser` 的 parser 负责解析。

**权衡**：约束越强，**失败重试**概率可能上升；需在「格式可靠」与「体验/延迟」间平衡。

---

## 3、上下文窗口、长上下文与记忆架构

> **模块重要度：⭐⭐⭐**（公开面试资料中极高频：**Context vs Memory**、长对话、成本）

### 8. 「上下文窗口」和「记忆」有什么区别？工程上如何分工？【⭐⭐⭐】

**上下文窗口**：模型单次推理能看到的 token 上限，按 token 计费、偏贵。**记忆**：应用层存储对话历史的组件，让 AI 能「记住」之前的对话。

**本仓库实现**：读历史 → 拼入 Prompt → 调模型 → 写回历史。用 RunnableWithMessageHistory + BaseChatMessageHistory（内存版 InMemory、持久化版 Redis）。**结论**：大块、长期、要检索的放外部记忆；窗口里只放高信号、短而准的上下文。

**常追问：Agent 的短期记忆和长期记忆你怎么设计？【⭐⭐⭐】**（与 **10**、**43** 同一故事线）

- **短期（会话级）**：当前 thread 最近 N 轮原文或滑动窗口；必要时**摘要 checkpoint** 写入同会话记录，保证多轮不丢约束。
- **长期（跨会话/用户级）**：用户画像、偏好、历史任务结论进 **DB/特征表**；体量大了用 **向量检索** 按当前意图召回相关记忆，而不是全塞进窗口。
- **与 RAG 区分**：RAG 是「企业知识库」；长期记忆是「用户/任务状态」。二者都要 **metadata + 权限**，防串租户。
- **更新策略**：记忆写入要**可版本化**、可人工纠正（尤其是客服/销售助手类）。

---

### 9. 什么是「Lost in the Middle」？对 RAG 有什么启示？【⭐⭐】

**现象**：长上下文中，模型对**开头和结尾**利用更好，**中间**关键证据容易被淹没（U 型注意力）。

**启示**：控制进窗片段数量、优先 rerank 再组装；重要结论放靠前或靠后；压缩/摘要减少中间噪声；必要时二次检索。

---

### 10. 超长多轮对话如何兼顾「不断档」与「省 token」？【⭐⭐】

与 **§8**「会话级记忆」配套：下面是具体手段。

1. **滑动窗口**：只保留最近 N 轮；风险是丢掉早期约束，需配合摘要。
2. **周期性摘要（Checkpoint Summary）**：每 K 轮把历史压成结构化摘要（目标、已确认事实、待办、用户偏好）。
3. **向量检索历史**：把旧轮次写入向量库，按当前问题动态召回相关片段，而非全量塞进窗口。
4. **结构化记忆槽**：如用户城市、会员等级等写入键值表，减少靠模型「回忆」。

---

### 11. 有了「超长上下文模型」，还需要 RAG 吗？【⭐⭐】

**通常仍需要**，理由可答：

- **成本与时延**：全量塞文档很贵、很慢；RAG 可先缩小证据集。
- **时效与权限**：企业知识分区、按租户过滤，RAG + metadata 更自然。
- **可溯源与合规**：引用片段、审计「用了哪段材料」比「全在窗口里」更易治理。

**补充**：长窗口更适合「**少量长文档内推理**」；企业海量知识仍多靠检索缩小范围。

---

## 4、RAG 与知识库

> **模块重要度：⭐⭐⭐**（对应 `2-RAG…`、`18`、`19`）

### 12. 描述一个完整的 RAG 流水线及关键模块。【⭐⭐⭐】

**标准流程**：

1. **索引阶段**：文档加载器 → 文本分割器（分块）→ 嵌入模型向量化 → 向量数据库写入。
2. **检索阶段**：用户提问 → 问题向量化 → 相似性检索（top_k）→ 取回相关片段。
3. **生成**：检索片段 + 用户问题 → 填入提示词模板 → 大模型生成 → 后处理（引用、脱敏）。

**核心**：RAG = 先查资料再作答，用真实文档约束生成、减轻幻觉。**追问**：chunk、metadata、多租户、索引更新（见下）。

**常追问：知识库更新怎么做？多租户怎么隔离？【⭐⭐】**

- **更新**：小变更**增量**（按文档 id 删旧向量再写入）；大版本可**全量重建**+蓝绿切换；要记录**索引版本**与**回滚**能力。注意**最终一致**（先写源库再异步索引 vs 同步阻塞）的取舍。
- **隔离**：向量与元数据带 **tenant_id**；检索必须 **filter**；禁止仅靠「拼 Prompt 告诉模型你是谁」做隔离——会串料。
- **权限**：敏感段落不进索引或进**独立集合**；生成侧再次校验**引用范围**。

---

### 13. RAG 效果不好时，你会如何排查？【⭐⭐】

**分层排查**：

1. **检索层**：查不到 / 召回噪声大 → 调 chunk、embedding、索引参数、hybrid、rerank、query 改写。
2. **上下文组装**：token 截断导致丢证据 → 压缩、摘要、分层检索；注意 **「Lost in the Middle」**（见 **9**）——无关片段过多时，关键证据即使在上下文中也可能被模型忽略。
3. **生成层**：不看上下文瞎编 → 强约束引用、降低温度、加「无证据则拒答」。
4. **数据层**：文档脏、过期、权限错误。

**RAG 常见 10 大坑（面试可脱口而出）**：

| 问题         | 典型原因               | 解决思路                               |
| ------------ | ---------------------- | -------------------------------------- |
| 内容缺失     | 切片不合理、召回率低   | 调分段长度、多向量检索、增量索引       |
| 错过重要文档 | 距离计算/排序不佳      | 优化相似度、引入 rerank                |
| 脱离上下文   | 多文档拼接语义丢失     | 语义加权拼接、Context Re-weighting     |
| 未能提取答案 | 阈值不当、Prompt 弱    | 调相似度阈值、强化「必须基于引用回答」 |
| 格式错误     | 数据清洗差、输出未约束 | 统一索引格式、Prompt 约束输出模板      |
| 领域特异性差 | 通用 embedding 不适配  | 领域词向量、知识图谱增强               |
| 回答不全面   | 召回范围小             | 多通道检索（keyword + embedding）      |
| 数据处理慢   | 非结构化提取慢         | 批量向量化、流式索引                   |
| 结构化数据弱 | 表格/SQL 支持差        | LLM 生成 SQL 再执行                    |
| PDF 提取难   | 表格、版式复杂         | 布局识别 + OCR + 坐标级切分            |

---

### 14. RAG 常见评测维度有哪些？【⭐⭐】

**检索**：Recall@K、MRR、nDCG（视工具链而定）。  
**生成**：Faithfulness（是否忠于检索内容）、Answer Relevance、Citation 正确率。  
**系统**：端到端延迟、成本、用户满意度。

**工具链提示**：开源/商用里常提到 **RAGAS、DeepEval、TruLens** 等框架做自动化指标；面试答出「检索指标 + 忠实度/相关性 + 端到端回归集」即可，不必死记单个库 API。

---

### 15. 稀疏检索与稠密向量检索如何取舍？Hybrid 适用场景？【⭐】

**简述**：稀疏（BM25）擅长大致关键词匹配；稠密擅长度义相似与 paraphrase。  
**Hybrid**：企业文档、混合中英文、专有名词多、用户 query 口语化时，常能稳态提升召回。

---

## 5、RAG 进阶：分块、GraphRAG、Agentic RAG

> **模块重要度：⭐ ～ ⭐⭐**（进阶岗/架构岗常问；与 `19` 章 RAG 实战可对照）

### 16. 文档分块（Chunking）有哪些策略？各有什么坑？【⭐⭐】

常用 **RecursiveCharacterTextSplitter**（按字符递归切分，尽量保持语义完整）。

| 策略           | 优点           | 典型坑                         |
| -------------- | -------------- | ------------------------------ |
| 固定字符/Token | 实现简单       | 切断表格/代码/条款，语义不完整 |
| 按标题/结构    | 边界较自然     | 依赖源文档结构质量             |
| 语义分块       | 语义完整性好   | 实现复杂、耗时                 |
| 滑动窗口+重叠  | 减少边界信息丢 | 存储与召回冗余上升             |

**面试收尾**：分块不是一次定终身，应用 **评测集 + badcase** 迭代；表格/代码常要 **专门解析器** 而非纯文本切分。

---

### 17. GraphRAG（图谱增强）适合什么场景？与传统向量 RAG 怎么选？【⭐】

**适合**：强 **实体-关系**、需要 **多跳推理**、社区/子图总结（如「某组织相关人物网络」）类问题。  
**代价**：建图、更新、存储与工程复杂度更高。

**传统向量 RAG**：适合大量非结构化段落、以「找相似段落」为主的问答。  
**实践**：可先向量 RAG 上线，确有大量多跳关系痛点再评估图谱路线。

---

### 18. 什么是 Agentic RAG？和「检索一次 + 生成」有何不同？【⭐⭐】

**Agentic RAG**：由 Agent 在循环中自主决定**是否检索、检索什么、是否换 query 再检**，甚至对检索结果自检后再检。**传统 RAG**：固定「检索一次 → 生成」单向管道。

**区别**：从单向管道变为带反馈的闭环（常与 ReAct/LangGraph 结合）。**风险**：更多模型轮次 → 成本、延迟、失控概率上升，必须配步数/预算/护栏。

---

### 19. RAG 有哪些常见「反模式」？【⭐】

与 **13** 分层排查同源，这里只列「原则性错误」：**chunk 极端**、**只做向量不做 hybrid/rerank**、**embedding 与领域不匹配却不评测**、**K 过大**（触发 **9**）、**无 metadata/ACL 致串租户**。

---

## 6、向量检索与 Embedding 选型

> **模块重要度：⭐**（工程面试常有一题；与 `18` 章呼应）

### 20. 向量数据库选型你会看哪些维度？【⭐】

**本仓库** 多用 Redis（Redis Stack）做向量存储。

**选型维度**：数据规模与 QPS、是否需要 metadata 过滤、延迟与可用性、多副本/云托管、成本、生态。LangChain 抽象为 VectorStore，具体有 Chroma、Pinecone、FAISS、Redis 等实现。

---

### 21. Embedding 模型与业务不匹配会怎样？如何验证？【⭐】

**现象**：召回差、跨语种漂移、领域词（医疗/法律/工单）向量距离不合理。

**应对**：在 **真实 query‑doc** 上抽评测集看 Recall/MRR；必要时换域内 embedding、微调向量模型、或对查询做 **同义词扩展/hybrid**。

---

## 7、框架选型与其他 SDK

> **模块重要度：⭐⭐**

### 22. LangChain、LangGraph、自研、低代码平台如何选型？【⭐⭐】

**LangChain vs Coze/Dify**（本仓库 9 章）：Coze/Dify 是**低代码平台**，拖拽搭建；LangChain 是**代码框架**，深度集成、强定制。二者可并存：平台做原型、对外应用；LangChain 做内部工具、复杂逻辑。

- **快速原型 / 标准工具链**：LangChain。
- **强状态、循环、人机协同、复杂分支**：LangGraph。
- **业务方主导、少研发**：Coze/Dify。
- **强管控与深度定制**：自研编排 + 统一治理层。

---

## 8、LangChain 与 Agent 运行时

> **模块重要度：⭐⭐⭐**（对应 `9`～`17`、`21`）

### 24. 如何使用 LangChain 开发一个 Agent？（原理级）【⭐⭐⭐】

**本仓库 V1.0 写法三步**：

1. **定义工具**：`tools = [get_weather]`，用 `@tool` 或封装函数，写清 name、description、参数 schema。
2. **构建 Prompt**：`system_prompt = "You are a helpful assistant."`。
3. **调用 create_agent**：`create_agent(model, tools, system_prompt)` 得到 agent，再 `agent.invoke({"input": "..."})`。

**原理**：Agent 通过 **ReAct 循环**（Thought → Action → Observation）完成「推理 → 行动 → 观察」直到结束。**Agent** 负责决定调哪个工具、何时结束；**AgentExecutor**（或 V1.0 底层 LangGraph）负责真正执行工具并把结果传回。**案例**：`AgentSmartSelectV1.0.py`、`AgentReact.py`。

---

### 25. LangChain Agent V0.3 与 V1.0 的主要差异是什么？【⭐⭐】

| 维度     | V0.x（如 AgentSmartSelectV0.3.py）                      | V1.0（如 AgentSmartSelectV1.0.py）                        |
| -------- | ------------------------------------------------------- | --------------------------------------------------------- |
| 创建方式 | Prompt 模板 + create_tool_calling_agent + AgentExecutor | 直接 `create_agent(model, tools, system_prompt)` 一步到位 |
| 提示词   | 必须用 PromptTemplate，含 agent_scratchpad              | 传字符串即可                                              |
| 调用     | `agent_executor.invoke`                                 | `agent.invoke`                                            |
| 底层     | 传统链                                                  | LangGraph 状态机                                          |

**面试可说**：本仓库有 V0.3 与 V1.0 同一业务（多城市天气比较）的对比案例，可现场演示差异。

---

### 26. 为什么「只有 Tool 不够，还需要 Agent」？【⭐⭐】

**核心**：**Tool = 能力**，**Agent = 决策 + 使用这些能力**。Tool 只提供「能做什么」，不负责「何时做、按什么顺序做、做到什么程度为止」。

**本仓库举例**：单步查天气 → 直接 Tool；「订明天北京到上海机票 + 查上海天气」→ Tool 不会自己决定先订票还是先查天气，需要 Agent 推理并编排；「PDF 总结 + 发邮箱」→ 需多个 Tool，谁先谁后要 Agent 决定。

---

## 9、LangGraph 与复杂工作流

> **模块重要度：⭐⭐⭐**（对应 `22`～`25`）

### 27. 在 LangGraph 中如何处理 Agent 的工具调用？【⭐⭐⭐】

- **节点化**：LLM 节点、工具节点分离，职责清晰。
- **条件边**：`add_conditional_edges` 根据模型输出（是否 tool call、哪条路由）跳转。
- **状态**：`StateGraph` + `TypedDict`/schema 管理共享状态，便于持久化与回放。
- **循环控制**：显式定义继续调用工具或结束的边条件，避免隐式死循环。

**为何用**：补 Chain 的线性、补「纯 Agent」的黑箱——用**显式图**换可控性（**28** 对比传统工作流）。案例：`案例与源码-3-LangGraph框架/`。

---

### 28. LangGraph 相比传统 workflow 引擎的优势？【⭐⭐】

**一句话**：把 **LLM 放进控制流**，同时保留**显式状态、分支、循环、checkpoint/HITL**（**27** 已述节点与条件边）。相对传统 BPMN 式固定流：更适配「模型在环」的不可预测路径；代价是**可观测与测试**要自建规范。案例见 `案例与源码-3-LangGraph框架/01-helloworld` 等。

---

### 29. LangChain / LangGraph 企业落地的常见挑战？【⭐⭐】

1. **可观测性**：多跳调用要 trace（每步输入输出、工具参数）。
2. **版本治理**：Prompt、工具 schema、图结构需版本化与灰度。
3. **延迟与成本**：多轮模型调用；需缓存、路由小模型、并行化。
4. **容错**：工具失败、格式错误、重试与降级策略。
5. **测试**：除单元测试外，需集成评测与回放固定会话。

---

### 30. Checkpoint、HITL 在工程上的价值？【⭐】

**Checkpoint**：持久化状态，长任务中断后可恢复、便于 replay 排错。**HITL**：敏感节点（支付、删数据）插入人工审批，确认后再继续。

---

## 10、低代码智能体平台（Coze / Dify）与工作流

> **模块重要度：⭐⭐**（对应 `3`、`4`～`7` 及案例；**业务落地岗**很爱问）  
> _注：本仓库以平台实战为主，下列题为通用面试抽象。_

### 31. Coze / Dify 这类平台与自研 LangChain/LangGraph 的边界怎么划分？【⭐⭐】

**平台优势**：可视化编排、权限与多租户、内置知识库/插件生态、上线快。  
**自研优势**：强定制流程、深度适配内部系统、复杂状态机/审计、与现有 CI/CD 一体化。

**选型**：POC / 业务迭代快 → 平台；核心链路长、合规与可编程要求高 → 代码化框架 + 自建治理。**本仓库**：3.1 ～ 3.10 为 Coze/Dify 案例（行业调研 PPT、客服分析、商品评论等）；4、5 章讲 API 调用与部署。

---

### 32. 用工作流做「智能体」时，和真正的 LLM Agent 有什么差别？【⭐⭐】

**工作流**：多数节点是**确定性**的，分支由规则或少量模型节点驱动，易测试；执行流程固定、结果可控。
**Agent**：模型在环中**反复决策**是否调工具，路径更动态，需更强的护栏与观测。

**实践**：复杂业务常采用 **「工作流编排 + 局部 Agent」** 的混合架构。Dify、Coze 等平台将工作流与 Agent 作为两种应用形态。

---

### 33. 如何通过 API 将平台工作流接入自有系统？【⭐】

**要点**：鉴权（API Key / OAuth）、异步与 webhook、幂等与重试、入参 schema 版本化、限流与超时；生产要记录 **run_id** 便于对账与排错。**本仓库**：第 4、5 章有 Dify/Coze 的 Python 调用示例；第 6、7 章为部署说明。

---

## 11、Function Calling / Tool Use

> **模块重要度：⭐⭐⭐**（对应 `17-Tools工具调用`）

### 34. Function Calling 的基本原理？【⭐⭐⭐】

**LLM 只负责输出「要调哪个工具、传什么参数」**，真正执行的是你的代码。

**五步流程**（本仓库 1-3 章有 DeepSeek API 演示）：① 告诉模型有哪些工具、用户说了什么；② 模型返回 tool_calls（name + arguments）；③ 代码执行工具拿到结果；④ 把结果塞回对话发给模型；⑤ 模型根据结果生成最终回复。

**本质**：模型产**调用计划**，**宿主负责真实世界副作用**与安全。工具定义用 `@tool`，参数可用 Pydantic；绑定模型用 `bind_tools`。

**常追问：ReAct 和 Function Calling 是什么关系？【⭐⭐⭐】**

- **ReAct**：一种**推理范式**——用自然语言写出 Thought → Action → Observation 的循环，强调「边想边做」。
- **Function Calling**：**协议/接口形态**——模型输出结构化的 `tool_calls`（工具名+参数），由宿主执行。
- **关系**：现代 Agent **常把二者结合**：循环仍是 ReAct 思想，但 Action 这一步用 **FC** 落地，便于解析、校验与观测；纯文本 Action 难稳定对接生产系统。

---

### 35. Function Calling 与「让模型输出 JSON 再自己解析」有何区别？【⭐⭐】

**原生 FC**：协议级约束、可靠性高、一次推理即可、平台/SDK 支持好。**纯文本 JSON**：易受话术干扰、需解析与修复、可能重试。**结论**：生产优先原生能力。

---

### 36. 在 RAG 系统里，Function Calling 可以做什么？【⭐⭐】

- Query 改写与多查询扩展；路由到不同知识库/工具；检索后处理（过滤、排序）；判断是否需要二次检索；调用计算器/结构化查询等。

**价值**：把固定 pipeline 变为按上下文动态组合的能力；由模型/Agent 决定检索策略而非固定「检索一次 → 生成」（见 Agentic RAG）。

---

### 37. 为什么说 Function Calling 是实现 AI Agent 的关键前提？【⭐⭐⭐】

**一句话**：没有可靠的「模型 → 结构化调用计划 → 宿主执行」这条链，大模型只能**空谈**，无法稳定地把决策变成**可观测、可审计、可组合**的外部动作；Function Calling（及等价的 Tool Use 协议）正是把这一步工程化、标准化的能力。

**分点可答**（合并表述）：① **从说到做**：结构化 `tool_calls` 才能驱动宿主执行 API/查库。② **能力边界**：实时数据、内部系统、副作用只能靠工具，FC 是**契约层**。③ **规划与执行分离**：便于权限、超时、重试、幂等与多工具编排（**34**、**43**）。④ **生态**：统一 schema 才能注册工具、接 **MCP（49）**、做版本治理。⑤ **安全**：宿主白名单 + schema 校验 + HITL，优于「模型直接吐可执行代码」。

**速记 + 痛点**：FC = **能执行**；多轮下易**臆造参数/忘步骤**，必须靠 **schema、状态机（如 LangGraph）、观测（63）** 兜底。

---

## 12、工具生态：幻觉、规模化与推理参数

> **模块重要度：⭐⭐**（Agent 落地与排错高频）

### 38. 什么是「工具幻觉」？如何缓解？【⭐⭐】

**含义**：模型调用不存在的工具、捏造参数、或在不允许写操作时强行走写路径（发生在 tool 空间）。

**缓解**：宿主白名单校验工具名；参数 JSON Schema/Pydantic 校验；工具描述清晰；过多工具时分层/分域或工具检索；高风险工具强制确认；失败时明确错误回传让模型重试（限次）。**生产**：意图识别（小模型/规则）→ 工具匹配（意图 →API 注册表）→ 置信度低时追问；异常回传模型 Self-Correction，连续失败转人工。

**补充：用提示词「手写 JSON 函数调用」时，常见幻觉表现**（无原生 FC 或老模型场景）：

| 类型           | 表现示例                                                         |
| -------------- | ---------------------------------------------------------------- |
| **函数名幻觉** | 约定 `get_weather`，模型输出 `get_forecast` 等不存在名字         |
| **参数名幻觉** | 约定 `city`，模型输出 `location` 等别名导致解析失败             |
| **格式幻觉**   | 要求 JSON 却缺引号、括号不匹配、夹杂自然语言                    |
| **类型/缺参**  | 该传 int 却传字符串；必填参数遗漏                               |
| **语义歧义**   | 「明天」被写成静态日期而非按当前时间计算                        |
| **安全话术**   | 被诱导输出本不该暴露的 token、密钥等（与 **61** 呼应）          |

**规避手段**：结构化 Prompt（示例 + JSON Schema 片段）、**两阶段**（先分类/抽取再调工具）、解析失败则 **限次重试**、宿主 **强校验** 与 **默认拒执行**、优先换用 **原生 Function Calling**（**35**）、关键路径 **HITL**。

---

### 39. 工具数量很多时，如何控制上下文与稳定性？【⭐⭐】

**思路**：

1. **分层**：Supervisor 先路由到子域，只在子域内暴露少量工具。
2. **动态工具选择（Tool Retrieval）**：先从库中检索 Top‑M 个工具描述进 Prompt，而非全量列出。
3. **聚合工具**：把细粒度 API 包装成少量「业务动作」工具，减少选择困难。
4. **文档与命名**：工具名可读、描述含「何时用/何时不用」，降低误选。

---

### 40. Temperature、top_p 在 Agent 系统里一般怎么配？【⭐】

**经验法则（非绝对）**：

- **选工具、抽参数、遵循严格格式**：偏低 temperature，减少随机胡选。
- **开放创作、营销话术**：可适当升高。
- **生产环境**：常按 **场景分配置**（路由/工具调用/最终润色分开），并 **固定 seed**（若平台支持）便于复现问题。

---

## 13、Agent 基础概念与设计

> **模块重要度：⭐⭐⭐**

### 41. Agent 和传统「单轮问答式」AI 应用有何不同？【⭐⭐⭐】

| 维度     | Agent 应用         | 传统问答         |
| -------- | ------------------ | ---------------- |
| 决策     | 可自主选用工具     | 多为一问一答     |
| 交互     | 多步、目标导向     | 常无状态或弱状态 |
| 能力扩展 | 插件化工具         | 功能固定         |
| 风险     | 工具滥用、成本失控 | 相对较低         |

**本仓库表述**：大模型只会「说」怎么调接口；智能体 = 大模型 + 真正去**执行**工具并把结果塞回对话。**Tool** 像工具箱里的螺丝刀、锤子；**Agent** 像有判断力的工匠——知道什么时候用、按什么顺序用。

---

### 42. Agent 适合解决哪类复杂任务？【⭐⭐】

**任务特征**：多步骤、信息在外部、需要试错与调整、存在分支与回溯。

**能力匹配**：

- **分解子任务**：将复杂目标拆成可执行步骤，而非一次性生成。
- **工具使用**：按需调用搜索、计算、API、数据库等外部能力。
- **迭代修正**：根据工具返回或用户反馈调整策略，而非一次定生死。
- **记忆与上下文延续**：跨轮次维护任务状态、用户偏好、中间结果。

**典型场景**：订票+查天气+汇总、多文档分析+邮件发送、研究类多源检索+报告生成。单步、规则清晰的任务用 Tool 或工作流即可，不必上 Agent。

---

### 43. 一个生产级 Agent 架构应包含哪些模块？【⭐⭐】

**核心模块**：感知（接收输入、意图粗分）→ 规划（ReAct/ToT 任务分解）→ 记忆（Redis 短期 + DB 长期）→ 工具（FC/MCP）→ 执行与反馈（工具执行器、错误处理）→ 评测与监控（trace、指标）→ 安全策略（白名单、HITL、脱敏）。

**落地要点**：记忆分层；规划有步数上限；工具调用审计留痕。

---

### 44. 如何设计 Agent 的可扩展性？【⭐⭐】

**工具层**：工具即插件，新能力通过注册新 Tool 实现，无需改核心逻辑；支持 MCP 动态发现。

**配置层**：Prompt、温度、步数上限、工具列表等外置配置或配置中心，支持灰度与 A/B。

**架构层**：无状态设计，会话状态存 Redis/DB，支持多实例水平扩展；复杂域拆为 **多 Agent**（Supervisor + 子 Agent），各司其职。

**治理层**：统一观测与注册中心，工具/Agent 版本化，便于回滚与问题复现。

---

### 45. 如何在 Agent 中增强推理能力？【⭐⭐】

- CoT；**ReAct**（Thought-Action-Observation）；Reflection；ToT；或 **LangGraph** 显式状态机降低「乱跑」概率。

**提醒**：推理链越长，**成本与延迟**越高，需上限控制。

---

### 48. Agent「调用工具」的完整流程怎么分点描述？（面试口述模板）【⭐⭐⭐】

在 **34**「五步」基础上，生产口述常把**宿主侧**拆细如下：

1. **输入**：用户问题 + 会话历史 + 系统提示（含安全与格式约束）+ **工具清单**（name、description、parameters schema）。
2. **推理**：模型判断是否需调工具；若不需要，直接自然语言回复并结束。
3. **产出调用计划**：模型输出结构化 **tool_calls**（工具名 + 参数 JSON）；宿主解析（失败则重试或降级）。
4. **宿主校验与执行**：校验工具白名单、参数 schema、权限与配额；执行工具（同步/异步），记录 trace（**63**）。
5. **回灌上下文**：将 **role=tool**（或厂商约定格式）的消息追加到对话，附上结果或错误信息。
6. **再推理**：再次调用模型，使其结合工具结果组织最终答案；若仍需多步则回到步骤 2～5。
7. **终止条件**：模型不再请求工具，或达到 **最大步数/最大工具次数/超时/预算**（**60**），或进入 HITL 节点。

**加分**：多工具时说明 **并行 vs 顺序**、失败 **Self-correction**、与 **MCP** 组合时工具来自远端发现（**53**）。**本仓库**：LangChain `create_tool_calling_agent` + `AgentExecutor`、V1.0 `create_agent` 均体现该闭环。

---

## 14、多智能体与 A2A

> **模块重要度：⭐ ～ ⭐⭐**（对应 `26-LangGraph多智能体与A2A`；**架构岗/大厂**问得多）

### 46. MCP 和 A2A 有什么本质区别？【⭐⭐】

- **MCP**：关注**工具访问**，定义大模型如何与工具、数据、资源交互；像「工具车间」——让工人知道每个工具的位置和用法。
- **A2A**：关注**代理协作**，定义智能体之间如何发现、通信、协作；像「会议室」——让不同专业代理能坐在一起协调任务。

**修车厂类比**：MCP 让维修工知道如何使用千斤顶、扳手；A2A 让客户能与维修工沟通，并让维修工之间或与配件商代理协作。

---

### 47. 多智能体有哪些常见拓扑？各自适用场景？【⭐⭐】

| 拓扑                    | 特点                        | 适用场景                                    |
| ----------------------- | --------------------------- | ------------------------------------------- |
| **单 Agent**            | 一个 LLM + 一组工具         | 简单对话、单一领域（天气、翻译、知识库 QA） |
| **Network**             | 多 Agent 平等、彼此可通信   | 多视角协作、并行检索汇总、研究讨论          |
| **Supervisor**          | 主控 Agent 调度子 Agent     | 企业助手（IT/HR/财务）、客服分诊            |
| **Supervisor as tools** | 主模型把子 Agent 当工具调用 | 子 Agent 像专业插件                         |
| **Hierarchical**        | 多层监督者                  | 大型任务拆解、复杂管道                      |
| **Custom**              | 自由组合 + HITL             | 强合规、需人工审批                          |

**本仓库案例**：`SupervisorV0.3.py`、`SupervisorV1.0.py`、`SupervisorHandoff.py`；A2A 案例 `Agent2Agent.py`（携程订机票、美团订酒店、滴滴打车，总协调按序调用）。

---

## 15、MCP（模型上下文协议）体系

> **模块重要度：⭐⭐⭐**（对应 `20-MCP模型上下文协议`；2025 年面试显著增多）

### 49. MCP 解决什么问题？与 Function Calling 的关系？【⭐⭐⭐】

**痛点**：各框架各自实现 Function Calling，缺乏统一标准；工具复用难。**MCP**：统一协议，规定工具如何暴露、如何被多端复用——可类比「AI 世界的 USB-C」。

**关系**：Function Calling 是「模型会调工具」的底层能力；MCP 是协议层，规定工具如何暴露与调用。二者组合：MCP 管理工具，模型通过 FC 触发。**本仓库**：`McpServer.py`、`McpClientAgent.py`。

---

### 50. MCP 的典型架构角色与传输方式？【⭐⭐】

| 角色              | 说明                                           |
| ----------------- | ---------------------------------------------- |
| **MCP 主机**      | 发起请求的 AI 应用（IDE、聊天宿主等）          |
| **MCP 客户端**    | 主机内与 Server **1:1** 连接                   |
| **MCP 服务器**    | 提供工具、资源、Prompt                         |
| **本地/远程资源** | Server 可访问的文件、DB、API                   |

**传输**：**STDIO** 适合本机/CLI/与主进程管道通信；**SSE（HTTP 长连）** 适合独立部署、多端访问。**选型口诀**：单机轻量 STDIO，服务化 SSE。案例见 `McpServer.py`、`McpServerByFastMCP.py`、`mcp.json`。

---

### 52. 开发 MCP Server 的一般流程？【⭐⭐】

1. **创建实例**：`mcp = FastMCP("服务名")`。
2. **注册工具**：`@mcp.tool()` 装饰函数，写清 name、description、参数类型。
3. **注册资源/提示**（可选）：`@mcp.resource(uri)`、`@mcp.prompt()`。
4. **启动**：`mcp.run(transport="stdio")` 或 `transport="sse"`。

**本仓库**：`McpServer.py`、`McpServerByFastMCP.py`、`McpServerWeatherByFastMCP.py` 可直接对照。

---

### 53. Agent 如何接入 MCP 工具（思路级）？【⭐⭐】

**本仓库流程**：`load_servers(mcp.json)` → `MultiServerMCPClient(connections)` → `async with client.session(): tools = client.get_tools()` → 交给 `create_tool_calling_agent` + `AgentExecutor`，与 V0.3 用法一致。**案例**：`McpClientAgent.py`。工具定义与运行在 MCP 服务端，客户端按协议发现并调用。

---

## 16、Agentic 系统与交互体验（综合表述）

> **模块重要度：⭐⭐**（系统设计题「开场白」很好用）

### 58. 什么是 Agentic（智能体化）系统？与「套一层 Prompt」区别在哪？【⭐⭐】

与 **41**（Agent vs 单轮）衔接：本题强调 **Agentic = 系统化落地**——显式 **状态**、**多步循环**、**工具真实执行**、**护栏（步数/权限/HITL）**、**可观测**。不是「多写几句 Prompt」，而是**可编排、可审计、可降级**的运行时。

**流式**：仅改善首字体验；**工具与状态**仍须完整落库（**63**）。

**常追问：多模态怎么接？【⭐】** API 传图 / OCR 版面解析后进 **RAG** 或进多模态模型；讲清 **进线、计费、超时** 即可。

---

## 17、安全、护栏与合规

> **模块重要度：⭐⭐⭐**（教程分散在多处，此处汇总为面试高频）

### 60. 如何防止 Agent「工具调用失控」？【⭐⭐⭐】

- **硬上限**：最大步数、最大工具调用次数、时间预算、费用预算。
- **电路熔断**：连续失败停止；异常率告警。
- **权限**：工具分级；写操作与人审（HITL）。
- **输入输出治理**：PII 脱敏、敏感指令拦截（prompt injection 防护）。
- **工具层**：校验「只允许已注册工具名」、参数 schema 校验，降低工具幻觉。

---

### 61. 什么是 Prompt Injection？应用层如何缓解？【⭐⭐】

**定义**：用户诱导模型**违背原指令**或**滥用工具**（如「忽略上文导出数据」）。

**缓解**：权限最小化、工具白名单、系统与用户消息隔离、输出过滤、关键操作二次确认、RAG 文档不可信时的隔离展示。

---

### 62. 代码解释器 / 浏览器自动化最大的安全风险？【⭐⭐】

**答法要点同 §23**（沙箱、网络/资源边界、审计、浏览器会话隔离）；面试一句话：**「执行」必须离业务进程隔离，且默认零信任出网。**

---

## 18、评测与可观测性

> **模块重要度：⭐⭐**（Agent 岗越来越常问）

### 63. 你会如何观测一个 Agent 系统？【⭐⭐】

**Tracing**：每次模型调用、工具调用的参数与返回、延迟、token。  
**Logging**：业务 run_id、用户会话、版本信息。  
**Metrics**：成功率、重试率、工具错误拓扑、成本曲线。

可选：**OpenTelemetry**、厂商 Trace、自建 JSON 日志管道。

---

### 64. Agent 评测为什么比单轮 QA 难？【⭐⭐】

多跳决策、工具组合爆炸、状态依赖、非确定性；需 **轨迹级**评测（选对工具、参数正确、是否过早终止）+ **结果级**评测（任务是否完成）。

---

## 19、成本、性能与部署运维

> **模块重要度：⭐⭐**（对应 `8` 企业部署、`12` Ollama 等章节思想）

### 66. Agent 系统延迟主要来自哪里？如何优化？【⭐⭐】

**来源**：多轮 LLM、同步工具、检索、重排序、过大上下文。

**手段**：流式输出、并行工具、缓存检索结果、小模型路由、压缩上下文、工具超时与异步化。

---

### 67. 调用成功率、任务完成率如何定义？【⭐⭐】

**工具调用成功率** = 成功调用次数 / 总调用次数（HTTP 2xx、无异常等）；**业务无结果**不一定算失败。

**任务完成率**：端到端是否达成用户目标（常需人工标注或用户确认），反映规划、工具覆盖与容错是否到位。

---

### 68. 线上运维关注哪些指标？【⭐⭐】

示例：**首 token 延迟**（P50/P95）、端到端时延、错误率、工具失败类型分布、每会话成本、用户满意度、回滚次数（模型/Prompt/索引）。

**常追问：Token 成本怎么控？【⭐⭐】**  
**答法**：**缓存**（同问题/同检索结果）、**小模型路由**（**69**）、**压缩上下文**（摘要、减 K）、**工具结果截断**、**按租户配额**；产品上限制单会话轮次与最大工具步数。指标上盯 **每会话 token、每任务成本、各路由分支占比**。

---

### 69. 模型路由怎么做？【⭐】

**分层策略**：

- **意图分类**：用小模型（如 7B）或规则（关键词/正则）做粗分，决定走 FAQ / RAG / Agent / 人工。
- **复杂度路由**：简单问答、格式固定 → 小模型；多步推理、开放生成 → 大模型。
- **成本优化**：热点问题缓存；能检索直接回答的不调用生成；按用户等级/场景分配配额。

**落地**：建立「意图 → 模型/流程」映射表，配合 A/B 测试调优；生产需监控各路由分支的延迟与成本占比。

---

## 20、垂直场景：Deep Research（选读）

> **模块重要度：⭐**（仅当 JD 含「研报 / 投研 / 深度搜索」时再细抠）

### 70. Deep Research 与单轮 RAG 差在哪？核心链路一句话？【⭐】

**一句话**：在 **Agent 循环里多次改 query、多源拉证据、再写作**，而不是「检一次 → 答一次」。**要点**：子问题规划 → 多源检索与去重冲突 → 迭代补检 → 长文+强制引用 → 关键结论可 **HITL**。与 **18（Agentic RAG）** 同一类思路，工程难点在 **成本、时延与防幻觉**。

---

## 21、垂直场景：NL2SQL Agent（选读）

> **模块重要度：⭐ ～ ⭐⭐**（JD 含「对话查数 / 智能报表」再精读）

### 74. 如何构建 NL2SQL Agent？如何保证正确性与安全？【⭐⭐】

**流水线**：Schema Linking（找表字段）→ SQL 生成 → 语法校验 → 沙箱/只读执行 → 结果解释 → 置信度与澄清。**Agent 优势**：多轮澄清、执行失败自愈、结合业务词典。

**安全**：Schema 约束、SQL 解析器校验、黑名单/白名单、只读账号、LIMIT、行级权限、Self-correction 限次重试。**歧义处理**：列名多义 → 列出候选让用户选择；时间模糊 → 反问具体区间；置信度低 → 展示预览请用户确认后再执行。**原则**：宁可多问一句。

---

## 22、垂直场景：智能客服（选读）

> **模块重要度：⭐⭐**（客服 / 运营助手类 JD 高频；纯内部 Agent 可略读架构要点）

### 78. 智能客服系统典型架构？高并发如何优化？【⭐⭐】

**链路**：意图识别 → 路由（FAQ / RAG / 工单 / 人工）→ 多轮澄清 → 回复与质检。**模块**：知识引擎、会话状态、人机协同、反馈闭环。**相对 FAQ 提升**：语义理解、多轮对话、个性化、复杂问题解决率更高。**RAG 价值**：FAQ 未命中时从手册/工单检索兜底；政策变更时更新索引即可。

**高并发**：水平扩展无状态服务、热点缓存、向量检索缓存、小模型做意图、大模型仅用于复杂生成、流式降低感知延迟。

---

## 23、垂直场景：代码解释器与浏览器（选读）

> **模块重要度：⭐**（JD 含「数据分析 Agent / 运维自动化 / 浏览器操作」再展开）

### 82～83. 代码执行与浏览器自动化：面试要答什么？【⭐】

**代码解释器**：必须 **沙箱**（容器或受限运行时）、**资源与网络白名单**、超时与输出上限、**全量审计**；与本地 REPL 差别在**隔离 + 可观测 + 与工具链集成**（与 **62** 呼应）。**浏览器 Agent**：Playwright 等控制层 + 页面理解 + 规划；靠 **重试、截图/DOM 断言、敏感站禁止自动登录** 提可靠性，连续失败 **降级或人工**。

---

## 24、面试实战：项目话术、选型与大厂高频清单

> **模块重要度：⭐⭐⭐**（项目经历、技术选型、跨栈协作——**应用岗必准备**）  
> **说明**：**§88** 中含营销/抽奖类问答；若简历无对应经历，可只保留其中 **Agent / MCP / RAG / 流式** 相关「问/答」，其余跳过。

### 84. 做 RAG 知识库实习/项目时，面试官常追问什么？怎么答？【⭐⭐⭐】

与 **8、12、13、69** 技术点交叉，下表侧重**「项目经历怎么讲」**；细节以正文为准。

| 追问方向           | 回答要点（技术侧）                                                                 |
| ------------------ | ---------------------------------------------------------------------------------- |
| **你具体负责哪块** | 索引管线（加载/清洗/分块）、Embedding 与向量库、检索策略、Prompt 与评测、或平台对接 |
| **幻觉与不准确**   | 检索质量（chunk、hybrid、rerank）、强制引用与拒答、温度调低、Self-check、人工抽检（见 **4**、**13**） |
| **小模型做前置筛选** | 意图分类、query 改写、是否走 RAG、粗排后再用大模型；降本 + 降噪（见 **69**）。面试官说的「小模型」常指 **端侧小 LLM**，与「传统 ML 小模型」概念不同，可主动对齐术语 |
| **检索 → 过滤 → 再生成** | 元数据/ACL 过滤、rerank、top‑k 与上下文预算控制，减少无关 token、提高忠实度（见 **9**、**13**） |
| **数据清洗与整理** | 去重、编码统一、脏 HTML/PDF 解析、表格/标题结构保留、权限字段打标                                           |
| **人工/规则分段**  | 按章节、条款、表格边界切分，或与算法分块结合；能举 badcase 驱动迭代（见 **16**）                          |
| **Python 熟练度**  | 大模型应用生态（LangChain、脚本、评测）以 Python 为主；Java 栈公司也常要求会写 Python 做 PoC 与对接 |

---

### 85. 面试问：为什么用 Java 做 Agent / RAG，而不是 Python？【⭐⭐⭐】

**一段说清楚**：教程与算法生态以 **Python** 为主（LangChain、LlamaIndex、PyTorch 等），但企业里**网关、鉴权、订单、MQ、现有运维**常在 **Java/Spring**。用 Java 做 **Agent/RAG 宿主**（对客 API、编排、审计、事务），用 **HTTP/gRPC/MQ** 调 Python 或云 API 做推理/离线索引，可**少一套生产栈、少跨语言排障**。若岗位偏算法，主动说 **双栈分工** 即可。

---

### 86. 这样介绍「通用 Agent 平台」项目可以吗？话术如何更稳？【⭐⭐】

**你的理解方向是对的**：强调**生产提效**（评审、需求/文档、日志分析等）、**多场景 = 多 Agent**、**通用工作流（分析‑执行‑监督‑总结）**、**配置化（模型/API/advisor/MCP/Prompt）入库 + 装配**，减少硬编码——这些都符合架构面试想听的「可扩展、可运营」。

**建议微调**：

1. **加一句边界**：「执行」若含副作用，一定强调**宿主校验、权限、审计、HITL**（**60**），避免听起来像「模型随便调生产接口」。
2. **Spring 装配**：若说「装配到 Spring 容器」，可补「配置变更走发布/灰度，与业务代码版本解耦」。
3. **与 MCP 对齐**：若平台接 MCP，一句带过「工具标准化暴露，多端复用」（**49**），很加分。

---

### 87. 为什么自研/手搓 RAG 编排，而不是直接用 Spring AI / LangChain4j？【⭐⭐】

**开场可说一句**：「是不是造轮子要看**当时决策点**上框架能否覆盖我们的向量库、多租户和索引更新节奏，而不是现在事后看生态。」

**标准答法**（按决策语境组织）：

1. **时间点与成熟度**：项目启动时目标框架版本、文档与生产案例是否满足**你们**的向量库、多租户、索引更新节奏。
2. **可控与排障**：核心链路（分块、检索、重排、引用）需要**逐层打点、定制 badcase 修复**；薄封装框架有时反而难深入。
3. **与现有系统一致**：日志、配置中心、熔断限流、事务与你们中间件风格统一，比硬套抽象层更省长期成本。
4. **不贬低框架**：可补「后续会评估把非差异化部分迁到 Spring AI / LangChain4j，以降低维护面」——体现工程理性。

---

### 88. 智能体平台 + 营销/抽奖：大厂面试问答汇编【⭐⭐⭐】

> **与正文重复时**：**MCP / 流式 / RAG 技术细节以 §3、§12、§15（49～53）为准**；本节保留**口述顺序**与**营销域**话术。无营销经历可只读平台架构、难点、MQ 通用部分。

---

**问**：介绍一下你的智能体平台？整体功能是什么？系统怎么设计实现的？代码怎么组织的？有哪些技术架构和功能模块、模块之间怎么交互？

**答**：**1 分钟结构**：背景一句 → 分层 → 核心模块 → 同步/异步交互 → 你负责的边界。

1. **定位**：面向内部或业务线的 **AI 应用平台**，把大模型产品化成「对话、知识问答、工具调用、工作流」，并与 **营销 / 运营**（活动、抽奖等）打通。
2. **技术架构**：**接入层**（网关、鉴权、限流、SSE/WebSocket）；**应用层**（Agent 编排、会话与 Prompt、租户隔离）；**能力层**（RAG、**MCP**、模型路由）；**领域层**（活动、奖池、库存、发奖）；**基础设施**（MySQL、Redis、**RabbitMQ**、向量库，按实际替换）。
3. **代码组织**：按 **限界上下文** 多模块；Agent 与营销域避免循环依赖；MQ 契约与 DTO 独立模块；模型/Prompt/工具 **配置与代码分离**，支持灰度。
4. **交互**：用户 **同步 HTTP** 进对话与抽奖核心；发奖、通知、对账等 **MQ 异步**；强一致扣减说明 **同步事务 / Redis 原子 / 最终一致**（与同节「库存防超卖」呼应）。

**收尾**：**trace_id** 全链路、配置发布与回滚——体现生产经验。

---

**问**：项目里遇到的最大难点是什么？怎么解决的？

**答**：用 **STAR**，准备 **2～3 个** 难点，每段 30～45 秒，**结果尽量量化**。

- **RAG 不稳**：badcase 归因分块/检索；hybrid、rerank、强制引用与拒答、评测集回归（亦见 **13**）。
- **流式 + 工具**：首包延迟、半次工具已执行；流式与状态机解耦、幂等与审计（亦见同节「流式对话」、**48**）。
- **抽奖一致性与 MQ**：超卖、重复抽奖、重试；幂等键、DB/Redis 扣减、对账（亦见同节「防超卖」「抽奖链路」）。
- **MCP/多模型**：schema 变更、超时熔断、降级（亦见 **49**、**60**）。
- **Java 集成大模型**：HTTP 超时重试、配额限流（亦见 **85**）。

---

**问**：RabbitMQ 在项目里怎么用的？MQ 在整个系统里起什么作用？

**答**：**一句话**：**异步解耦、削峰、最终一致**——把「必须马上答复用户」和「可以慢慢做完」分开。

1. **抽奖/发奖**：接口只做资格校验、落抽奖结果、同步返回；发券、积分、通知等 **投递 MQ**，消费者重试与对账。
2. **活动事件**：活动上下线、配置变更，**路由/广播** 给库存预热、缓存失效、风控刷新等订阅方。
3. **Agent 长任务**：总结、报告生成等 **入队**，Worker 调模型，避免占满 Web 线程。
4. **分工**：参与次数、防重、关键扣减多在 **DB/Redis 同步**；可补偿链路走 **MQ**。

**加分**：交换机类型、**死信队列**、消费者 **业务幂等键**；**丢消息**（生产者确认、持久化、集群策略按实际）与 **重复消费**（幂等、去重表）能说清。

---

**问**：营销抽奖平台有哪些功能模块？整体结构怎么划分？

**答**：**活动配置**（规则、时间窗、人群、奖品与概率、预算）→ **奖池与库存**（日限额、黑白名单）→ **抽奖引擎**（权重随机、防刷、幂等）→ **参与与中奖记录**（对接账户/订单）→ **发奖履约**（异步、重试、补单）→ **运营与风控**（监控、熔断、审计对账）。结构上 **配置 + 同步核心交易 + MQ 异步履约**；与 Agent 可表述为「咨询走对话/RAG，**下单抽奖仍走营销核心**」，边界清晰。

---

**问**：为什么做这个项目？

**答**（选 2 条组合）：**业务**上活动多、规则多变，要统一能力、降故障；**效率**上用 Agent+RAG 减轻文档、配置、排障、客服辅助；**技术**上沉淀可复用 AI 平台，避免各业务重复接模型；**个人**上希望在营销高并发与 LLM 工程交叉领域有可讲清的架构经验。避免空泛「追热点」，落到 **痛点与可度量目标**。

---

**问**：为什么会考虑用 RabbitMQ（而不是别的消息中间件）？

**答**：先 **场景** 再 **选型**：业务偏 **任务队列、路由订阅、中等吞吐**，团队熟悉 AMQP、公司有现成集群与规范。**与 Kafka 对比**：Kafka 偏 **高吞吐日志、可回放**；RabbitMQ 偏 **复杂路由、低延迟任务**——发奖/通知多消费者场景 RabbitMQ 更好讲。**收尾**：体量再大时可评估 Kafka/Pulsar，体现演进思维。

---

**问**：你平时用 AI 吗？用过哪些工具？怎么用的？

**答**：**真实、具体**。编码用 Cursor/Copilot 等辅助写样板、单测、读仓库，**自己 review 与跑测试**；学习用模型 **对照官方文档** 理解 MCP、LangChain 等；原型可用 Dify/Coze，复杂逻辑用 **代码化 Agent**；写 Prompt 交代背景、格式与边界，**产出二次校验**。Java 岗可补：用 AI **辅助读 Spring AI 文档**，实现代码仍自己核对线程与超时。

---

**问**：说一下你对 MCP 的理解？大模型是怎么与 MCP 交互的？你们怎么实现的？

**答**：口径同 **49～53、48**：USB‑C 类比 + Client/Server + 工具进 FC 闭环；工程上补 **鉴权、超时、schema 版本、trace**。Spring AI 类名以**当前文档**为准，只讲 **ToolCallback + 传输层** 原理。

---

**问**：流式对话怎么实现的？断连重试打算怎么做？

**答**：口径同 **§3**（stream/SSE、心跳、request_id、降级）+ 本仓库 `LLM_Stream.py`、`25-LangGraph高级特性.md`。强调 **工具执行与流式解耦**、半次调用可审计（**60**）。

---

**问**：RAG 向量检索你们怎么做的？怎么优化？

**答**：口径同 **12、13、9、69**（流水线 + hybrid/rerank + 控 K + badcase）。口述时加 **1 个你经手的优化例子**。

---

**问**：说一下抽奖的完整流程（从用户参与到出结果、发奖）。

**答**：**校验**（登录、活动有效、人群、次数上限、风控）→ **同步扣资格/占库存**（幂等）→ **服务端按权重随机**（多套概率见下一问），禁止纯前端随机 → **落库** 流水与中签、带 **业务幂等号** → **同步返回** 结果（券码等可「发放中」）→ **MQ 异步履约**（发券、短信、第三方），失败重试/死信/人工补偿 → **日终对账**。

---

**问**：库存/奖品怎么防止超卖？

**答**：先说明「超卖」指哪一层（活动总量、日库存、单用户限额）。**手段**：DB `UPDATE … WHERE stock>0` 看影响行数或 **乐观锁**；Redis **DECR/Lua** 原子扣减，注意 **Redis 与 DB 一致性**与 **宕机预案**；表上 **唯一索引** 防重复参与/发奖；必要时单奖池 **串行消费**（慎用、牺牲吞吐）。可补 **压测 QPS** 与 Redis 故障降级策略。

---

**问**：一套活动里多种抽奖概率一般怎么做？

**答**：**配置驱动**：**权重表** + 归一化 **roll**；多套概率 = 多套配置 + 按渠道/标签/AB 选配置。**分层**：首抽安慰奖 + 后续大奖可用 **状态机** 或两次随机。**预算**：大奖 **日限额** 先查剩余再抽，没有则 **降级** 兜底。**可测**：固定种子单测；配置 **审核与版本号**。

---

**问**：限流是怎么做的？

**答**：**网关** 全站 QPS/连接；**应用** 令牌桶/漏桶（Sentinel、Resilience4j、Guava 等），维度 **用户/设备/IP/活动**；**Redis** 滑动/固定窗口 + Lua；对用户返回 **友好错误码**，核心链路配合 **降级与熔断**；活动前 **压测与预热**。

---

**问**：Spring AI 底层是怎么实现 MCP 的？（偏难）

**答**：**不背死类名**：MCP 落在 **Tool Calling 集成层**，**Client** 连 Server，工具元数据映射为 **ToolCallback**，经 **ChatClient** 注册后仍走 **FC**；搞清 **STDIO/SSE 传输**、**JSON‑RPC 风格消息**、**工具 discovery** 即可。承认细节以**当前版本文档/源码**为准，可提关注 **Spring AI Alibaba / 官方 sample**，避免编造被追问穿帮。

---

**本节冲刺要点**

1. **架构 + 模块 + 数据流**能口头画一遍；**难点**备 2～3 个带数据的故事。
2. Agent 侧重 **MCP、RAG、流式**（协议、优化、观测）。
3. 营销侧重 **超卖、Redis 与 MySQL 一致性、Redis 高可用、限流降级、MQ 丢消息与重复消费**。

---

## 附录 C：新手入门与运行案例

> 面试若被问「有没有跑过项目」，可参考本仓库快速上手。

**环境**：Python 3.10 ～ 3.13；虚拟环境 `python3.10 -m venv .venv`；`pip install -r requirements.txt`。

**API Key**：复制 `.env-example` 为 `.env`，填入 `aliQwen-api`、`deepseek-api` 等；变量名需与代码一致。**运行**：在**项目根目录**执行 `python 案例与源码-2-LangChain框架/01-helloworld/StandardDesc.py`，否则可能找不到 `.env`。

**本地模型**：Ollama 可本地运行大模型，无需 API Key。

**案例索引**：完整案例与源码在 `案例与源码-2-LangChain框架/`、`案例与源码-3-LangGraph框架/` 等目录。

---

## 附录 A：速记表

### A1 ReAct 循环

**Thought → Action → Observation** 直到满足结束条件。四步：思考（形成计划）→ 行动（选工具、传参）→ 观察（接收工具返回）→ 循环或结束。

### A2 失败模式清单（面试常用来「展示经验」）

目标含糊、工具描述不清、检索噪声、工具返回非结构化、无步数上限、无权限隔离、无观测导致不可复盘。

### A2.5 本仓库核心概念速记

| 概念                 | 主要作用                                              |
| -------------------- | ----------------------------------------------------- |
| **Tool**             | 能力的封装，大模型能「用」的原子能力                  |
| **Function Calling** | 大模型「决定调哪个、传什么参数」并解析返回的底层能力  |
| **RAG**              | 通过检索把相关文档注入 prompt，让模型「看到」更多信息 |
| **MCP**              | 规定工具/数据源如何暴露、如何被发现的标准化协议       |
| **Agent**            | 负责何时用哪个 Tool、按什么顺序、何时结束的决策层     |
| **Context / Memory** | **Context** = 当次窗口（贵）；**Memory** = 外存+可检索（**8**）。长窗口 ≠ 替代 RAG；防 **lost in the middle（9）** |

### A3 推荐答题结构（系统设计题 / 项目深挖）

**场景与约束** → **数据流与状态** → **关键算法/模型** → **工具与权限** → **评测与监控** → **成本与降级**。  
**应用岗加一句**：上线后 **谁改 Prompt、谁改索引、如何回滚**——考官常借此判断你有没有真干过生产。

---

## 附录 B：题库索引与冲刺建议

> 面向 **AI 智能体 / 大模型应用开发工程师**：优先 **RAG + Agent + FC/MCP + 工程治理**，算法训练细节次之。题号保留空隙为历史编号，不影响阅读。

### B0 岗位冲刺路径（建议顺序）

1. **第一天（核心）**：**1、2、4、8**（含 Agent 记忆追问）、**12**（含索引/租户）、**13、34、37、48、49、53、58、60、63**  
2. **第二天（框架与编排）**：**24、27、29、38、39、41、43、66、68**（含 token 成本）、**69**  
3. **第三天（项目与表达）**：**84～88**（按简历裁剪）、复盘 **3**（含 API 治理追问）  
4. **按需选读**：**31～33**（Dify/Coze）、**70、74、78、82～83**（看 JD）

### B1 高频索引（速查）

- **基础与选型**：0.1、1、2、3（含 **云上 API 治理**）、4、5  
- **上下文与记忆**：8（含 **短期/长期记忆**）、9、10、11  
- **RAG**：12（含 **更新与多租户**）、13～21、**18**（Agentic RAG）  
- **编排与框架**：22、24～30、31～33  
- **FC / 工具**：34（含 **ReAct 关系**）、35～40、**48**  
- **Agent 与 MCP**：41～47、49～53（**50** 含架构角色与 STDIO/SSE 选型）、58  
- **安全与治理**：60～64  
- **成本与运维**：66～69  
- **垂直（选读）**：70、74、78、82～83  
- **面试实战**：84～88  

### B2 与「网上面经」常见题的对照

| 面经常见问法           | 本文优先看     |
| ---------------------- | -------------- |
| RAG 为啥不准、怎么排查 | **13、12**     |
| 幻觉怎么处理           | **4、13**      |
| Agent 和 Chatbot 区别  | **41、58**     |
| 工具调不好 / 乱调      | **38、48、60** |
| MCP 是啥、为啥要       | **49～53**     |
| 流式、断连、观测       | **3**、**88**、**63** |
| 微调和 RAG 选型        | **1、2**       |
| 多智能体怎么拆         | **47**         |
